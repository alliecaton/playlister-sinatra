<!DOCTYPE html>
<html>
<h1>Edit Song</h1>
<form action='/songs/<%=@song.slug%>' method=POST>
<input type="hidden" name=_method value=PATCH>

<h3><%=@song.name%></h3>

    <br><label for="artist_name">Artist Name</label><br>
        <input type="text" id="artist_name" name="artist[name]" value="<%=@artist%>"> <br><br>

    <label>Genres</label><br><br>
    <%Genre.all.each do |genre|%>
        <input type="checkbox" id="<%=genre.name%>" name="genres[]" value="<%=genre.id%>" <%='checked' if @song.genres.include?(genre)%> >
        <label><%=genre.name%></label><br>
    <%end%>

    <input type="submit" value="Save">

</form>


</html>